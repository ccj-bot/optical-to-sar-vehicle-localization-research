# PERSON CMR-v0 R04 independent confirmation

- Active workspace: `D:\profile\research\workspace`
- Interpreter: `D:\MINICONDA\envs\py311\python.exe`
- Output: `D:\profile\research\workspace\output\person_physics_guided_image_domain_study_20260824\cmr_v0_r04_independent_confirmation`
- `old_work`: archive-only and not used

This task performs the one-shot held-out R04ZF confirmation of the frozen
CMR-v0 common/residual mechanism.  It freezes the confirmation protocol,
evaluator, structurally matched controls, and hashes before reference reveal.
It preserves continuous descriptors, uncertainty, frozen categorical states,
graded candidate separation, strict SAR-edge outcomes, cluster-aware summaries,
offline likely-supported branch analysis, and deterministic multimodal review.

It does not modify CMR-v0, P0, q95, topology, timing, GMC, uncertainty,
residual states, or cross-modal relation rules.  It cannot produce pruning,
identity assignment, a tracker, P2, a final SAR center, or a final SAR box.

## Completed confirmation result

- One-shot R04 reference reveal completed without mechanism modification.
- `98` eligible windows / `166` branch instances; `3` reference-supported SAR edges; `6` frozen supported-vs-wrong comparisons from `2` evaluable windows in one target/temporal cluster.
- Candidate separation: strong `0`, asymmetric `0`, tendency `2`, none `2`, reversed `2`; one window contains positive tendency separation and the other contains reversal/harm.
- Strict outcomes: SAR-edge rescue `0`, confirmation `4`, harm `2`, conflict `0`, no-information `0`.
- Scene-common prefers the supported primary in all `6/6`; branch residual is non-decisive and sometimes harmful.
- Strict branch-specific evaluation remains unavailable; all `6` comparisons are offline likely-supported exploratory grounding.
- Direct visual review completed. The primary route is `KEEP_CMR_AS_NONDECISIVE_DYNAMIC_EVIDENCE`; any branch-residual redesign must use new development data, not R04.
- Final independent validation: `PASS (64/64)`.
