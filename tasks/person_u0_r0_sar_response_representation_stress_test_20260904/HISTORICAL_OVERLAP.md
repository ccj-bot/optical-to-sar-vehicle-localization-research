# U0-R0 Historical Overlap

| Direction considered | Already tried? | Earlier purpose and limitation | What is genuinely new here? |
|---|---|---|---|
| Q90/Q95/Q97.5 connected components | Yes, PERSON P1E produced all three levels on 398 development frames. | It measured shell burden/reference support; it did not treat cross-threshold parentage as the object question. | Recompute the same levels on the four frozen T0 windows, build exact parent-child relations, and ask which T0 ambiguity changes representation. |
| Multi-threshold persistence / split / merge | Yes, S1-L used 0.35/0.50/0.65 structure observations and temporal events. | Background controls reproduced persistence; many split/merge pages coincided with fragmentation bursts or crop boundaries. It was vehicle-thread research with a different field and support contract. | Test the fixed PERSON C2/S(x) field, unchanged T0 corridors, and threshold-specific P0 families; report cross-representation invariants rather than event counts. |
| Manual response atlas / response object | Yes, RSA0 manually separated skeleton, definite/probable response, background, mixed, and unresolved zones. | Geometry was manual/GT-mapped and covered two short vehicle windows; it did not yield a deployable automatic object definition. | Use no seed or reference in construction and stress current automatic components in W1/W3/W4/W5. |
| Local line/ridge primitive extension | Yes, RSA1-A0 extended 12 px primitives from a manual seed. | Partially supported in PV002 and failed frozen replay in PV003; seed dependence and background barriers remained. | Not rerun. A new local-max/ridge score would duplicate prior work and violate the no-new-candidate-score boundary. Q97.5 components are used only as existing-field peak cores. |
| Aspect-conditioned component lineage | Yes, RSA2-G2/R2B recorded fragmented bright bands, component cards, and lineage relations. | Conclusions were GT-local, scene/aspect conditional, and explicitly not a general search/localization rule. | Examine whether current PERSON strict-family changes are already visible as threshold-dependent component lineage without using aspect, GT geometry, or identity. |
| GM17 SAR structure factor / structure-only ranking | Yes. | It tested local display-image structure descriptors on an existing candidate bank and remained display-image limited. | Not rerun: U0-R0 defines response support units and forbids ranking/score fusion. |
| TERG-v1 set-valued bundle | Yes. | It repaired graph semantics above Q95 physical nodes but retained Q95 components as the physical-node authority. | Stress the underlying Q95 node definition itself; a stable graph over unstable nodes is not assumed correct. |

Stop rule applied: local-max/NMS candidate extraction and a new ridge score are not
executed because materially similar directions already exist and would not
answer the new representation question.
